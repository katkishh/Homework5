<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/imageViewImage"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/image_add_size"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="4dp"
        android:scaleType="center"
        android:src="@drawable/media"
        app:layout_constraintEnd_toEndOf="@+id/imageViewCancelImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/imageViewCancelImage" />

    <ImageView
        android:id="@+id/imageViewCancelImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_image_cancel"
        app:layout_constraintEnd_toEndOf="@+id/imageViewImage"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>